# AMR Robot Planning — 여기서 시작

이 프로젝트를 사용할 때는 먼저 `AMRRobotPlanning.prj`를 연다. 그 다음 목적에 따라 아래
파일 하나만 선택하면 된다.

## 1. 지금 두 Stateflow 레이아웃을 비교하려는 경우

MATLAB Command Window에서 실행한다.

```matlab
compare_mission_supervisor_layouts("MissionRegion")
```

이 명령은 다음 두 모델의 같은 Subchart를 함께 연다.

- `models/mission_supervisor/versions/amr_mission_supervisor_v06a_centered_readable_curved_2026_07_29.slx`
- `models/mission_supervisor/versions/amr_mission_supervisor_v06b_centered_readable_minimum_curvature_2026_07_29.slx`

다른 Subchart는 `MissionRegion` 대신 `NavigationRegion`, `SafetyRegion`, `HealthRegion`,
`EnergyRegion`을 입력한다. 자세한 비교 방법은
[`models/mission_supervisor/comparison/README.md`](models/mission_supervisor/comparison/README.md)에 있다.

## 2. 현재 정식 Mission Supervisor를 보려는 경우

```text
models/mission_supervisor/amr_mission_supervisor.slx
```

이 파일은 중앙 정렬과 저장 화면 가독성 검증을 통과한 `v06b`의 복사본이다.

## 3. 전체 통합 시스템을 실행하려는 경우

```text
models/integrated_system/amr_integrated_delivery_system.slx
```

대표 회귀검증은 다음 명령으로 실행한다.

```matlab
run_integrated_environment_matrix()
```

## 폴더를 어떻게 이해하면 되는가

| 폴더 | 의미 | 일반 사용자가 볼 필요 |
| --- | --- | --- |
| `models/mission_supervisor` | Supervisor 정식본·버전·비교 자료 전체 | 있음 |
| `models/integrated_system` | 전체 AMR 통합 실행 모델 | 있음 |
| `models/examples` | 단계별 학습·실험 모델 | 학습할 때만 |
| `scripts` | 실행·검증·모델 생성 함수 | 명령을 찾을 때 |
| `docs` | 설계·이론·검증 문서 | 설명이 필요할 때 |
| `results` | MATLAB 실행이 만든 원시 결과 | 보통 없음 |
| `work` | 백업·디버그·중간 생성물 | 없음 |
| `resources`, `slprj` | MATLAB Project 메타데이터와 생성 캐시 | 없음 |

`work`, `resources`, `slprj`의 파일은 직접 열지 않는다. 사용자에게 필요한 모델은 모두
`models` 아래에 있다.

지금까지 어떤 순서로 바뀌었는지는
[`models/mission_supervisor/versions/README.md`](models/mission_supervisor/versions/README.md)의
`v01 → v06a/b` 표에서 확인한다.
